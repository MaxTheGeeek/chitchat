<!DOCTYPE html>
<html>
  <%- include('./partials/head.ejs') %>

  <body class="bg-dark">
    <div class="centered-form">
      <div class="centered-form__box">
        <h5 class="text-center">Please select your room</h5>
        <form action="/chat" method="POST">
          <!-- <input
            class="text-light rounded-pill my-5"
            type="text"
            name="username"
            placeholder="Display name"
            autocomplete="off"
            required
          /> -->
          <div class="col-6 mx-auto my-3">
            <h5 class="text-center">Display name:</h5>
            <h4 class="text-warning text-center text-capitalize">
              <%= username %>
            </h4>
          </div>
          <input
            class="text-light rounded-pill mt-5"
            type="text"
            name="room"
            placeholder="Room"
            autocomplete="off"
            required
          />
          <button class="btn btn-info rounded-pill my-5">Join</button>
        </form>
      </div>
    </div>
  </body>
</html>
